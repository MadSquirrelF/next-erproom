import { memo } from "react";

interface OrgschemaMenuRoutesProps {
  className?: string;
}

export const OrgschemaMenuRoutes = memo((props: OrgschemaMenuRoutesProps) => {
  const { className } = props;

  return <div>OrgschemaMenuRoutes</div>;
});
