"use client";
import { memo } from "react";

interface ActionManageScreenFileProps {
  className?: string;
}

export const ActionManageScreenFile = memo(
  (props: ActionManageScreenFileProps) => {
    const { className } = props;

    return <div>ActionManageScreenFile</div>;
  },
);
